export class TimeLineDesc {
  date: string;
  summary: string;
  title: string
  constructor(  date: string, summary: string, title: string) {
    this.date = date;
    this.summary = summary
    this.title = title;
  }
}
